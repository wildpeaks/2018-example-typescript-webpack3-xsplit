/* eslint-env node, jasmine */
import {example} from '.';

describe('Function "example"', () => {
	it('Dummy test', () => {
		let throws = false;
		let actual;
		try {
			actual = example(100);
		} catch(e){
			throws = e.message;
		}
		expect(throws).toBe(false);
		expect(actual).toBe(101);
	});
});
